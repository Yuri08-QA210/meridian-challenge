services:
  - type: web
    name: meridian-ctf
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: FLAG
        sync: false
      - key: MAX_QUERIES
        value: "10000"
      - key: PORT
        value: "10001"
    plan: free
