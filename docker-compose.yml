version: '3.8'

services:
  meridian:
    build: .
    ports:
      - "10001:10001"
    environment:
      - FLAG=QA{r3d4ct3d}
      - MAX_QUERIES=10000
      - PORT=10001
    restart: unless-stopped
